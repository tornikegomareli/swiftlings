// operators1.swift
//
// Swift has all the basic arithmetic operators you'd expect.
// Let's practice using them!
//
// Fix the calculations to get the expected results.

// I AM NOT DONE

func main() {
    let a = 10
    let b = 3
    
    // TODO: Fix these calculations
    let sum = a - b        // Should be 13
    let difference = a + b // Should be 7
    let product = a / b    // Should be 30
    let quotient = a * b   // Should be 3
    
    print("Sum: \(sum)")
    print("Difference: \(difference)")
    print("Product: \(product)")
    print("Quotient: \(quotient)")
    
    // TODO: Calculate the remainder when a is divided by b
    let remainder = 0  // Should be 1
    print("Remainder: \(remainder)")
}